# Place a copy of this config to ~/.aerospace.toml
# After that, you can edit ~/.aerospace.toml to your liking

# It's not necessary to copy all keys to your config.
# If the key is missing in your config, "default-config.toml" will serve as a fallback

# You can use it to add commands that run after login to macOS user session.
# 'start-at-login' needs to be 'true' for 'after-login-command' to work
# Available commands: https://nikitabobko.github.io/AeroSpace/commands
after-login-command = []

# You can use it to add commands that run after AeroSpace startup.
# 'after-startup-command' is run after 'after-login-command'
# Available commands : https://nikitabobko.github.io/AeroSpace/commands
after-startup-command = [
  'exec-and-forget sketchybar',
  # JankyBorders: Catppuccin colors (blue=active, grey=inactive)
  'exec-and-forget borders active_color=0xff8aadf4 inactive_color=0xff939ab7 width=4.0'
]

# Notify Sketchybar about workspace change AND swap per-workspace wallpaper.
# wallpaper-workspace.sh looks for ~/Pictures/Wallpapers/workspace-<NAME>.{jpg,png}
# with a graceful fallback chain — missing files are a no-op, never a crash.
exec-on-workspace-change = ['/bin/bash', '-c',
    'sketchybar --trigger aerospace_workspace_change FOCUSED_WORKSPACE=$AEROSPACE_FOCUSED_WORKSPACE; ~/scripts/wallpaper-workspace.sh "$AEROSPACE_FOCUSED_WORKSPACE" & ~/dotfiles/scripts/scripts/kitty-font-per-workspace.sh "$AEROSPACE_FOCUSED_WORKSPACE" &'
]

# Notify when mode changes (v0.19+) - useful for visual feedback
# This runs AeroSpace commands when switching modes (e.g., entering resize mode)
# To trigger external scripts, use exec-and-forget:
# on-mode-changed = ['exec-and-forget sketchybar --trigger mode_change MODE=$AEROSPACE_MODE']
# For now, we leave it empty - uncomment above line if you add mode indicator to sketchybar
on-mode-changed = ['exec-and-forget sketchybar --trigger aerospace_mode_change MODE=$AEROSPACE_MODE']

# Start AeroSpace at login
start-at-login = true

# Unhide hidden apps when focusing them
automatically-unhide-macos-hidden-apps = true

# Normalizations. See: https://nikitabobko.github.io/AeroSpace/guide#normalization
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

# See: https://nikitabobko.github.io/AeroSpace/guide#layouts
# The 'accordion-padding' specifies the size of accordion padding
# You can set 0 to disable the padding feature
accordion-padding = 30

# Possible values: tiles|accordion
default-root-container-layout = 'tiles'

# Possible values: horizontal|vertical|auto
# 'auto' means: wide monitor (anything wider than high) gets horizontal orientation,
#               tall monitor (anything higher than wide) gets vertical orientation
default-root-container-orientation = 'auto'

# Possible values: (qwerty|dvorak)
# See https://nikitabobko.github.io/AeroSpace/guide#key-mapping
key-mapping.preset = 'qwerty'
# Mouse follows focus when focused monitor changes
# Drop it from your config, if you don't like this behavior
# See https://nikitabobko.github.io/AeroSpace/guide#on-focus-changed-callbacks
# See https://nikitabobko.github.io/AeroSpace/commands#move-mouse
# on-focus-changed = "move-mouse window-lazy-center"
on-focused-monitor-changed = ['move-mouse monitor-lazy-center']

# Gaps between windows (inner-*) and between monitor edges (outer-*).
# Possible values:
# - Constant:     gaps.outer.top = 8
# - Per monitor:  gaps.outer.top = [{ monitor.main = 16 }, { monitor."some-pattern" = 32 }, 24]
#                 In this example, 24 is a default value when there is no match.
#                 Monitor pattern is the same as for 'workspace-to-monitor-force-assignment'.
#                 See: https://nikitabobko.github.io/AeroSpace/guide#assign-workspaces-to-monitors
[gaps]
inner.horizontal = 15
inner.vertical = 15
outer.left = 15
outer.bottom = 15
# outer.top =        40
outer.right = 15
# Per-monitor top gap. Update to current monitor names detected by `aerospace list-monitors`.
# Adjust values as desired per monitor; last number is the default fallback.
outer.top = [
  { monitor."Built-in Retina Display" = 15 },
  { monitor."PORTRAIT-MONITOR" = 50 },
  15,
]

# Pin key workspaces to monitors
# See: https://nikitabobko.github.io/AeroSpace/guide#assign-workspaces-to-monitors
[workspace-to-monitor-force-assignment]
# Built-in display can't be renamed in BetterDisplay, use actual macOS name
1 = 'Built-in Retina Display'  # Main workspace on laptop
2 = '^PORTRAIT-MONITOR$'       # Secondary workspace on portrait
B = 'Built-in Retina Display'  # Browser on laptop
D = 'Built-in Retina Display'  # Development on laptop
F = 'Built-in Retina Display'  # Finder on laptop (floats)
M = 'Built-in Retina Display'  # Messaging on laptop
N = 'Built-in Retina Display'  # Notes on laptop
T = '^PORTRAIT-MONITOR$'       # Terminal on portrait
# 'main' binding mode declaration
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
# 'main' binding mode must be always presented
[mode.main.binding]

# All possible keys:
# - Letters.        a, b, c, ..., z
# - Numbers.        0, 1, 2, ..., 9
# - Keypad numbers. keypad0, keypad1, keypad2, ..., keypad9
# - F-keys.         f1, f2, ..., f20
# - Special keys.   minus, equal, period, comma, slash, backslash, quote, semicolon, backtick,
#                   leftSquareBracket, rightSquareBracket, space, enter, esc, backspace, tab
# - Keypad special. keypadClear, keypadDecimalMark, keypadDivide, keypadEnter, keypadEqual,
#                   keypadMinus, keypadMultiply, keypadPlus
# - Arrows.         left, down, up, right

# All possible modifiers: cmd, alt, ctrl, shift

# All possible commands: https://nikitabobko.github.io/AeroSpace/commands

# You can uncomment this line to open up terminal with alt + enter shortcut
# See: https://nikitabobko.github.io/AeroSpace/commands#exec-and-forget
# alt-enter = 'exec-and-forget open -n /System/Applications/Utilities/Terminal.app'

# See: https://nikitabobko.github.io/AeroSpace/commands#layout
alt-slash = 'layout tiles horizontal vertical'
alt-comma = 'layout accordion horizontal vertical'
alt-period = 'layout floating tiling'
alt-shift-space = 'fullscreen'
alt-r = 'reload-config'
# alt-s sketchybar toggle moved to Karabiner (Ctrl+Shift+\) — terminals swallow Option key
alt-0 = 'workspace-back-and-forth'
alt-shift-c = 'close'
alt-enter = '''exec-and-forget open -na /Applications/Ghostty.app'''

# See: https://nikitabobko.github.io/AeroSpace/commands#focus
alt-h = 'focus --boundaries-action wrap-around-the-workspace left'
alt-j = 'focus --boundaries-action wrap-around-the-workspace down'
alt-k = 'focus --boundaries-action wrap-around-the-workspace up'
alt-l = 'focus --boundaries-action wrap-around-the-workspace right'

# See: https://nikitabobko.github.io/AeroSpace/commands#move
alt-shift-h = 'move left'
alt-shift-j = 'move down'
alt-shift-k = 'move up'
alt-shift-l = 'move right'
alt-shift-minus = 'resize smart -50'
alt-shift-equal = 'resize smart +50'

# Swap windows (v0.19+): exchange position with adjacent window
# See: https://nikitabobko.github.io/AeroSpace/commands#swap
alt-ctrl-h = 'swap left'
alt-ctrl-j = 'swap down'
alt-ctrl-k = 'swap up'
alt-ctrl-l = 'swap right'

# DFS Navigation (v0.19+): cycle through ALL windows depth-first
# Useful for quickly cycling when you have many windows
# See: https://nikitabobko.github.io/AeroSpace/commands#focus
alt-rightSquareBracket = 'focus dfs-next'
alt-leftSquareBracket = 'focus dfs-prev'
# See: https://nikitabobko.github.io/AeroSpace/commands#workspace
alt-1 = 'workspace 1'
alt-2 = 'workspace 2'
# alt-3 = 'workspace 3'
# alt-4 = 'workspace 4'
# alt-5 = 'workspace 5'
# alt-6 = 'workspace 6'
# alt-7 = 'workspace 7'
# alt-8 = 'workspace 8'
# alt-9 = 'workspace 9'
alt-b = 'workspace B' # for browser
# COMMENTED OUT FOR BRAZILIAN ACCENTS - ROLLBACK: uncomment line below
# alt-e = 'workspace E' # for email
alt-f = 'workspace F' # for finder
alt-d = 'workspace D' # for development
alt-m = 'workspace M' #
# COMMENTED OUT FOR BRAZILIAN ACCENTS - ROLLBACK: uncomment line below
# alt-n = 'workspace N' # for notes
# alt-p = 'workspace P'
alt-t = 'workspace T' # for terminal shell
# alt-v = 'workspace V'

# See: https://nikitabobko.github.io/AeroSpace/commands#move-node-to-workspace
alt-shift-1 = 'move-node-to-workspace 1'
alt-shift-2 = 'move-node-to-workspace 2'
# alt-shift-3 = 'move-node-to-workspace 3'
# alt-shift-4 = 'move-node-to-workspace 4'
# alt-shift-5 = 'move-node-to-workspace 5'
# alt-shift-6 = 'move-node-to-workspace 6'
# alt-shift-7 = 'move-node-to-workspace 7'
# alt-shift-8 = 'move-node-to-workspace 8'
# alt-shift-9 = 'move-node-to-workspace 9'
alt-shift-b = 'move-node-to-workspace B'
# COMMENTED OUT FOR BRAZILIAN ACCENTS - ROLLBACK: uncomment line below
# alt-shift-e = 'move-node-to-workspace E'
alt-shift-f = 'move-node-to-workspace F'
alt-shift-d = 'move-node-to-workspace D'
alt-shift-m = 'move-node-to-workspace M'
# COMMENTED OUT FOR BRAZILIAN ACCENTS - ROLLBACK: uncomment line below
# alt-shift-n = 'move-node-to-workspace N'
# alt-shift-p = 'move-node-to-workspace P'
alt-shift-t = 'move-node-to-workspace T'
# alt-shift-v = 'move-node-to-workspace V'
# See: https://nikitabobko.github.io/AeroSpace/commands#workspace-back-and-forth
alt-tab = 'workspace-back-and-forth'
# See: https://nikitabobko.github.io/AeroSpace/commands#move-workspace-to-monitor
alt-shift-tab = 'move-workspace-to-monitor --wrap-around next'

# See: https://nikitabobko.github.io/AeroSpace/commands#mode
# COMMENTED OUT FOR BRAZILIAN ACCENTS - ROLLBACK: uncomment line below
# alt-shift-semicolon = 'mode service'

alt-shift-r = 'mode resize'
alt-shift-x = 'mode bd-mode'

[mode.resize.binding]
# Fine control (50px)
h = 'resize width -50'
j = 'resize height +50'
k = 'resize height -50'
l = 'resize width +50'

# Coarse control (200px)
shift-h = 'resize width -200'
shift-j = 'resize height +200'
shift-k = 'resize height -200'
shift-l = 'resize width +200'

b = 'balance-sizes'

# Exit resize mode
enter = 'mode main'
esc = 'mode main'
# 'service' binding mode declaration.
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
[mode.service.binding]
esc = ['reload-config', 'mode main']
r = ['flatten-workspace-tree', 'mode main'] # reset layout
#s = ['layout sticky tiling', 'mode main'] # sticky is not yet supported https://github.com/nikitabobko/AeroSpace/issues/2
f = [
  'layout floating tiling',
  'mode main',
] # Toggle between floating and tiling layout
backspace = ['close-all-windows-but-current', 'mode main']

alt-shift-h = ['join-with left', 'mode main']
alt-shift-j = ['join-with down', 'mode main']
alt-shift-k = ['join-with up', 'mode main']
alt-shift-l = ['join-with right', 'mode main']

# BetterDisplay mode-switching chord. Entry: alt-shift-x. Single key picks the mode.
[mode.bd-mode.binding]
w = ['exec-and-forget /Users/lgertel/dotfiles/scripts/scripts/bd-apply.sh dawn', 'mode main']
d = ['exec-and-forget /Users/lgertel/dotfiles/scripts/scripts/bd-apply.sh day', 'mode main']
a = ['exec-and-forget /Users/lgertel/dotfiles/scripts/scripts/bd-apply.sh afternoon', 'mode main']
e = ['exec-and-forget /Users/lgertel/dotfiles/scripts/scripts/bd-apply.sh evening', 'mode main']
n = ['exec-and-forget /Users/lgertel/dotfiles/scripts/scripts/bd-apply.sh night', 'mode main']
m = ['exec-and-forget /Users/lgertel/dotfiles/scripts/scripts/bd-apply.sh meeting', 'mode main']
r = ['exec-and-forget /Users/lgertel/dotfiles/scripts/scripts/bd-apply.sh read', 'mode main']
s = ['exec-and-forget /Users/lgertel/dotfiles/scripts/scripts/bd-apply.sh stream', 'mode main']
c = ['exec-and-forget /Users/lgertel/dotfiles/scripts/scripts/bd-apply.sh cinema', 'mode main']
esc = 'mode main'
enter = 'mode main'

[[on-window-detected]]
if.app-id = 'com.apple.finder'
run = 'layout floating'  # Opens on current screen, not forced to workspace F

[[on-window-detected]]
if.app-name-regex-substring = 'Shottr'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.mitchellh.ghostty'
run = "move-node-to-workspace T"

# Playwright browser - keep floating in current workspace
[[on-window-detected]]
if.app-name-regex-substring = 'Chromium'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.google.Chrome'
run = "move-node-to-workspace B"

[[on-window-detected]]
if.app-id = 'app.zen-browser.zen'
run = "move-node-to-workspace B"

[[on-window-detected]]
if.app-id = 'company.thebrowser.Browser'
run = "move-node-to-workspace B"

[[on-window-detected]]
if.app-id = 'company.thebrowser.dia'
run = "move-node-to-workspace T"

[[on-window-detected]]
if.app-id = 'com.apple.Safari'
run = "move-node-to-workspace B"

[[on-window-detected]]
if.app-id = 'org.mozilla.firefox'
run = "move-node-to-workspace B"

# Duplicate fallback Finder mapping removed (handled above with float + move)

[[on-window-detected]]
if.app-id = 'notion.id'
run = "move-node-to-workspace N"

[[on-window-detected]]
if.app-id = 'md.obsidian'
run = "move-node-to-workspace N"

[[on-window-detected]]
if.app-id = 'com.apple.Notes'
run = "move-node-to-workspace N"

[[on-window-detected]]
if.app-id = 'com.openai.chat'
run = "move-node-to-workspace N"

[[on-window-detected]]
if.app-id = 'com.anthropic.claudefordesktop'
run = "move-node-to-workspace N"

[[on-window-detected]]
if.app-id = 'ai.perplexity.mac'
run = "move-node-to-workspace N"

[[on-window-detected]]
if.app-id = 'com.electron.wispr-flow'
run = "move-node-to-workspace N"

# Terminals → T
[[on-window-detected]]
if.app-id = 'com.googlecode.iterm2'
run = "move-node-to-workspace T"

[[on-window-detected]]
if.app-id = 'org.alacritty'
run = "move-node-to-workspace T"

[[on-window-detected]]
if.app-id = 'com.github.wez.wezterm'
run = "move-node-to-workspace T"

# Messaging → M
[[on-window-detected]]
if.app-name-regex-substring = 'slack|discord|telegram|teams'
run = "move-node-to-workspace M"

# Float small utility apps
[[on-window-detected]]
if.app-id = 'com.1password.1password'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'pro.betterdisplay.BetterDisplay'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.logi.cp-dev-mgr'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.elgato.StreamDeck'
run = 'layout floating'

# Explicit WhatsApp → M
[[on-window-detected]]
if.app-id = 'net.whatsapp.WhatsApp'
run = "move-node-to-workspace M"

# IDEs → Development workspace (D) on Built-in display
[[on-window-detected]]
if.app-id = 'com.microsoft.VSCode'
run = ['layout tiling', 'move-node-to-workspace D']

[[on-window-detected]]
if.app-id = 'com.cursor.cursor'
run = ['layout tiling', 'move-node-to-workspace D']

[[on-window-detected]]
if.app-id = 'com.todesktop.230313mzl4w4u4'
run = ['layout tiling', 'move-node-to-workspace D']

[[on-window-detected]]
if.app-id = 'com.todesktop.230313mzl4w4u92'
run = ['layout tiling', 'move-node-to-workspace D']

[[on-window-detected]]
if.app-id = 'com.google.android.studio'
run = ['layout tiling', 'move-node-to-workspace D']

[[on-window-detected]]
if.app-id = 'com.apple.dt.Xcode'
run = ['layout tiling', 'move-node-to-workspace D']

[[on-window-detected]]
if.app-name-regex-substring = 'intellij|clion|pycharm|webstorm|rider|goland|datagrip|rubymine|phpstorm|fleet'
run = ['layout tiling', 'move-node-to-workspace D']

# Docker Desktop → floating (accessible from any workspace)
[[on-window-detected]]
if.app-id = 'com.electron.dockerdesktop'
run = 'layout floating'

# Email → E
[[on-window-detected]]
if.app-id = 'com.readdle.SparkDesktop-setapp'
run = ['layout tiling', 'move-node-to-workspace E']

[[on-window-detected]]
if.app-id = 'com.apple.mail'
run = ['layout tiling', 'move-node-to-workspace E']
