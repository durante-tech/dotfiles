# Stow package manifest — THE single source of truth.
#
# Read by install.sh (stow -R), setup.sh (check_stow_drift) and
# .github/workflows/lint.yml (stow dry run). Those three used to carry three
# hand-maintained copies of this list, and they had already drifted: CI was
# dry-run-verifying 15 of the 22 packages, so a stow conflict in espanso,
# fastfetch, kitty, mise, ubersicht, wallpapers or wezterm could ship green.
#
# One name per line. Blank lines and #-comments are ignored.
#
# NOT listed here, deliberately:
#   launchagents/  — .plist.template files rendered by setup.sh --configure
#   raycast/       — script-commands symlinked by setup.sh link_raycast_commands
#   macos/         — defaults scripts, executed not stowed
#   site/          — Astro docs site, not deployed to $HOME
#   ubersicht/     — the APP is still in use (and still installed by install.sh,
#                    still started at login by com.lucas.ubersicht). Only these
#                    CUSTOM WIDGETS were retired, 2026-08-26: Lucas does not run
#                    them, so ~/Library/Application Support/Übersicht/widgets is
#                    now a plain empty directory the app owns. The 11 widgets stay
#                    in the repo for reference.
#                    Un-stowing them also cleared a batch-restow blocker: the live
#                    target was a hand-made ABSOLUTE symlink (setup.sh rewrites
#                    stow's relative one, which Übersicht's server.js cannot
#                    follow), stow refuses to adopt an unowned target, and one
#                    such target aborts ALL packages rather than just its own.
#                    To redeploy: re-add `ubersicht` below, `stow -t ~ ubersicht`,
#                    then re-run setup.sh --configure to re-absolutise the link.
#
# Entries inside a package that must not be stowed (aerospace/templates,
# wallpapers shaders, zsh docs) are excluded by that package's own
# .stow-local-ignore, not from here.

aerospace
atuin
espanso
fastfetch
ghostty
karabiner
kitty
linearmouse
mise
mpd
nvim
rmpc
scripts
sketchybar
starship
tmux
w3m
wallpapers
yazi
zsh
